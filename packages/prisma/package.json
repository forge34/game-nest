{
  "name": "@gridcollect/prisma",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "dependencies": {
    "@prisma/client": "^6.15.0"
  },
  "files": [
    "dist"
  ],
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "db:push": "pnpm prisma db push",
    "db:reset": "pnpm prisma db push --force-reset",
    "db:generate": "pnpm prisma generate --sql && pnpm build"
  },
  "devDependencies": {
    "prisma": "^6.15.0",
    "typescript": "^5.9.2"
  }
}
